module Enums
{
  export enum AuditTypes
  {
    StockIssue = 0,
    StockAdd = 1,
    StockUpdate = 2,
    StockAdjust = 3,
    StockRemove = 4,
    StockGroupAdd = 5,
    StockGroupUpdate = 6,
    StockGroupRemove = 7
  }

  export enum Events
  {
    
  }
}

export = Enums;
