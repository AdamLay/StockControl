-
    var formatDate = function(d) {
        var now = new Date();

        var str = "";

        if (now.toLocaleDateString() == d.toLocaleDateString()) {
            
            var diffSecs = (now - d) / 1000;

            if (diffSecs < 60)
                return Math.floor(diffSecs) + " second" + (Math.floor(diffSecs) == 1 ? "" : "s") + " ago";

            var diffMins = diffSecs / 60;

            if (diffMins < 60)
                return Math.floor(diffMins) + " minute" + (Math.floor(diffMins) == 1 ? "" : "s") + " ago";

            var diffHours = diffMins / 60;

            return Math.floor(diffHours) + " hour" + (Math.floor(diffHours) == 1 ? "" : "s") + " ago";

        } else {

            str += d.getHours() + ":" + d.getMinutes() + " ";
            str += d.getDate() + "/" + (d.getMonth() + 1);
        }
        return str;
    }

.panel.panel-info
    .panel-heading
        i.fa.fa-bell
        |  Notifications
    #lstNotifications.list-group
        each item in notifications
            a.list-group-item(href="/audit?id=#{item.Id}")
                i(class="#{getIcon(item.Title)}")
                |  #{item.Title}
                span.pull-right.text-muted.small(data-time="#{new Date(item.Timestamp).getTime()}") #{formatDate(new Date(item.Timestamp))}